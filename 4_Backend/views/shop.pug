extends layouts/main-layout.pug

block styles
    link(rel="stylesheet", href="/css/home.css")
block content
    main 
        if prods.length > 0
            div
                each product in prods
                    h1 #{product.title}
        else
            h1 Sorry, products to be loaded!!